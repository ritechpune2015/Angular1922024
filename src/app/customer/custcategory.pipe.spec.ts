import { CustcategoryPipe } from './custcategory.pipe';

describe('CustcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new CustcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
